class Solution {
public:
int missingNum(vector<int>& arr) {
int n = arr.size() + 1; // Total elements including the missing one
int total = n * (n + 1) / 2;
int sum = 0;
for (int i = 0; i < arr.size(); ++i) {
sum += arr[i];
}
return total - sum;
}
};